Fixes:
- The Discord agent picker now lists the agents you have used, rather than every agent TokenBar recognises. It offered a row for agents you have never run and for ones you had hidden, and naming one of those left the presence empty without saying why. The agent your setting already names stays listed even if you hide it, so the picker keeps showing what the setting points at.
